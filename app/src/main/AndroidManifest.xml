<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.mainproject">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ViewProfileActivity" android:label="View Profile"></activity>
        <activity android:name=".AddIngredientActivity" android:label="Add Ingredient"/>
        <activity android:name=".IngredientsActivity" android:label="My Ingredients"/>
        <activity android:name=".RegisterActivity" android:label="Register New User"/>
        <activity android:name=".RecipeCreatorActivity" android:label="Recipe Creator"/>
        <activity android:name=".LoginActivity" android:label="Hop Journal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MyRecipesActivity" android:label="My Recipes"/>
        <activity android:name=".SettingsActivity" />
        <activity android:name=".AddPostActivity" android:label="Create New Post"/>
        <activity android:name=".ProfileActivity" android:label="My Profile"/>
        <activity android:name=".SplashActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>